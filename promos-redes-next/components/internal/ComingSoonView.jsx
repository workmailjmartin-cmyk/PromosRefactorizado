export default function ComingSoonView({ titulo }) {
  return (
    <div style={{ maxWidth: '600px', margin: '60px auto', textAlign: 'center', color: '#555' }}>
      <h2 style={{ color: '#11173d' }}>{titulo}</h2>
      <p>Esta sección se migra en una fase siguiente. Por ahora seguí usando el sistema actual para esta función.</p>
    </div>
  );
}
